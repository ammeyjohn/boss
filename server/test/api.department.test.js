var assert = require('assert');
var deptApi = require('../api/department.js');

describe('API', function() {
    describe('Department', function() {
        describe('addDeparment', function() {
            it('Should return the new department object with integer id.', function() {
                var DEPT = {

                };
                //var new_dept = deptApi.addDeparment()
                assert.notEqual();
            });
        });
    });
});